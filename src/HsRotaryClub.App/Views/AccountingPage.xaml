<UserControl x:Class="HsRotaryClub.App.Views.AccountingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具列 -->
        <DockPanel Grid.Row="0" Margin="0,0,0,6">
            <TextBlock Text="會計月報表" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding StatusMessage}" Margin="20,0" VerticalAlignment="Center" Foreground="Gray"/>

            <TextBlock Text="年度:" Margin="20,0,4,0" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBox Text="{Binding Year}" Width="60" Margin="0,0,8,0"/>

            <TextBlock Text="月份:" Margin="0,0,4,0" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBox Text="{Binding Month}" Width="40" Margin="0,0,8,0"/>

            <Button Content="重新載入" Command="{Binding ReloadCommand}" Padding="8,4" Margin="4,0"/>
            <Button Content="新增科目" Command="{Binding AddSubjectCommand}" Padding="8,4" Margin="4,0" Background="#A0C4FF"/>
        </DockPanel>

        <!-- 中: 左收入 + 右支出 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock DockPanel.Dock="Top" Text="📈 收入科目" FontWeight="Bold" FontSize="14" Margin="0,0,0,4"/>
                <DataGrid ItemsSource="{Binding IncomeRows}"
                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="科目代號" Binding="{Binding Code}" Width="80"/>
                        <DataGridTextColumn Header="科目名稱" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="本月收入" Binding="{Binding ThisMonth, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="累計收入" Binding="{Binding Ytd, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="預算收入" Binding="{Binding AnnualBudget, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="執行率" Binding="{Binding ExecutionRateText}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="#EEE"/>

            <DockPanel Grid.Column="2">
                <TextBlock DockPanel.Dock="Top" Text="📉 支出科目" FontWeight="Bold" FontSize="14" Margin="0,0,0,4"/>
                <DataGrid ItemsSource="{Binding ExpenseRows}"
                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="科目代號" Binding="{Binding Code}" Width="80"/>
                        <DataGridTextColumn Header="科目名稱" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="本月支出" Binding="{Binding ThisMonth, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="累計支出" Binding="{Binding Ytd, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="預算支出" Binding="{Binding AnnualBudget, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="執行率" Binding="{Binding ExecutionRateText}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Grid>

        <!-- 底部合計 -->
        <Border Grid.Row="2" Background="#F0F0F0" Margin="0,6,0,0" Padding="6,3">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="收入合計: 本月 " Foreground="Gray"/>
                <TextBlock Text="{Binding IncomeThisMonthTotal, StringFormat=N0}" FontWeight="Bold" Foreground="DarkGreen" Margin="0,0,4,0"/>
                <TextBlock Text=" / 累計 " Foreground="Gray"/>
                <TextBlock Text="{Binding IncomeYtdTotal, StringFormat=N0}" FontWeight="Bold" Foreground="DarkGreen" Margin="0,0,4,0"/>
                <TextBlock Text=" / 預算 " Foreground="Gray"/>
                <TextBlock Text="{Binding IncomeBudgetTotal, StringFormat=N0}" Margin="0,0,20,0"/>
                <TextBlock Text="支出合計: 本月 " Foreground="Gray"/>
                <TextBlock Text="{Binding ExpenseThisMonthTotal, StringFormat=N0}" FontWeight="Bold" Foreground="DarkRed" Margin="0,0,4,0"/>
                <TextBlock Text=" / 累計 " Foreground="Gray"/>
                <TextBlock Text="{Binding ExpenseYtdTotal, StringFormat=N0}" FontWeight="Bold" Foreground="DarkRed" Margin="0,0,4,0"/>
                <TextBlock Text=" / 預算 " Foreground="Gray"/>
                <TextBlock Text="{Binding ExpenseBudgetTotal, StringFormat=N0}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>