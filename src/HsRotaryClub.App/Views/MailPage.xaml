<UserControl x:Class="HsRotaryClub.App.Views.MailPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,6">
            <TextBlock Text="信件作業" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding StatusMessage}" Margin="20,0" VerticalAlignment="Center" Foreground="Gray"/>

            <Button Content="新增信件" Command="{Binding AddJobCommand}" Padding="8,4" Margin="20,0,4,0" Background="#A0C4FF"/>
            <Button Content="儲存" Command="{Binding SaveJobCommand}" Padding="8,4" Margin="4,0" Background="#FFD27F"/>
            <Button Content="刪除" Command="{Binding DeleteJobCommand}" Padding="8,4" Margin="4,0"/>
        </DockPanel>

        <!-- 中: 上 MailJob + 下 收件人 -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="4"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <DockPanel Grid.Row="0" Margin="0,0,0,4">
                    <TextBlock Text="📬 信件清單" FontWeight="Bold" FontSize="14"/>
                </DockPanel>
                <DataGrid Grid.Row="1" ItemsSource="{Binding Jobs}" SelectedItem="{Binding SelectedJob}"
                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="主旨" Binding="{Binding Subject}" Width="*"/>
                        <DataGridTextColumn Header="排程" Binding="{Binding ScheduleType}" Width="80"/>
                        <DataGridTextColumn Header="建立時間" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="140"/>
                        <DataGridTextColumn Header="上次寄送" Binding="{Binding LastSentAt, StringFormat=yyyy-MM-dd HH:mm}" Width="140"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <GridSplitter Grid.Row="1" Height="4" HorizontalAlignment="Stretch" Background="#EEE"/>

            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <DockPanel Grid.Row="0" Margin="0,0,0,4">
                    <TextBlock Text="👥 收件人 (選取的信件)" FontWeight="Bold" FontSize="14"/>
                    <Button Content="+ 從社員自動加入" Command="{Binding AddRecipientsFromMembersCommand}" Padding="8,4" Margin="20,0,4,0" Background="#A0C4FF"/>
                </DockPanel>
                <DataGrid Grid.Row="1" ItemsSource="{Binding Recipients}"
                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="社員編號" Binding="{Binding MemberCode}" Width="80"/>
                        <DataGridTextColumn Header="社員姓名" Binding="{Binding MemberName}" Width="100"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                        <DataGridTextColumn Header="狀態" Binding="{Binding Status}" Width="100"/>
                        <DataGridTextColumn Header="錯誤訊息" Binding="{Binding ErrorMessage}" Width="200"/>
                        <DataGridTextColumn Header="寄送時間" Binding="{Binding SentAt, StringFormat=yyyy-MM-dd HH:mm}" Width="140"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>

        <Border Grid.Row="2" Background="#F0F0F0" Margin="0,6,0,0" Padding="6,3">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="待寄: " Foreground="Gray"/>
                <TextBlock Text="{Binding PendingCount}" Margin="0,0,20,0"/>
                <TextBlock Text="已寄: " Foreground="Gray"/>
                <TextBlock Text="{Binding SentCount}" Foreground="DarkGreen" Margin="0,0,4,0"/>
                <TextBlock Text="筆" Margin="0,0,20,0"/>
                <TextBlock Text="失敗: " Foreground="Gray"/>
                <TextBlock Text="{Binding FailedCount}" Foreground="DarkRed" Margin="0,0,4,0"/>
                <TextBlock Text="筆" Margin="0,0,20,0"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="DarkGreen"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>