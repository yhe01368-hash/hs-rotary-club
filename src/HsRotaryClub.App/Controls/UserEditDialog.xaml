<Window x:Class="HsRotaryClub.App.Controls.UserEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="使用者編輯" Width="400" Height="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Microsoft JhengHei, Microsoft YaHei, Segoe UI"
        FontSize="14">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="帳號"      Grid.Row="0" Grid.Column="0" Margin="0,8" VerticalAlignment="Center"/>
        <TextBox   x:Name="UsernameBox"  Grid.Row="0" Grid.Column="1" Margin="0,4" Padding="4"/>

        <TextBlock Text="顯示名稱" Grid.Row="1" Grid.Column="0" Margin="0,8" VerticalAlignment="Center"/>
        <TextBox   x:Name="DisplayBox"   Grid.Row="1" Grid.Column="1" Margin="0,4" Padding="4"/>

        <TextBlock Text="角色"      Grid.Row="2" Grid.Column="0" Margin="0,8" VerticalAlignment="Center"/>
        <ComboBox  x:Name="RoleBox" Grid.Row="2" Grid.Column="1" Margin="0,4" Padding="4">
            <ComboBoxItem Content="Admin"/>
            <ComboBoxItem Content="Treasurer"/>
            <ComboBoxItem Content="Member"/>
        </ComboBox>

        <TextBlock x:Name="PasswordLabel" Text="密碼" Grid.Row="3" Grid.Column="0" Margin="0,8" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Grid.Column="1" Margin="0,4" Padding="4"/>

        <TextBlock x:Name="ErrorText" Foreground="Red" FontSize="12"
                   Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                   Margin="0,8" TextWrapping="Wrap" Visibility="Collapsed"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                    Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,12,0,0">
            <Button x:Name="OkButton" Content="確定" Width="100" Padding="6,4" Margin="4"
                    Background="#1A4789" Foreground="White" FontWeight="Bold" Click="Ok_Click" IsDefault="True"/>
            <Button Content="取消" Width="100" Padding="6,4" Margin="4" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
